<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/dist/output.css" rel="stylesheet" />
    <title>Edit Contact</title>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto my-8">
      <div class="bg-white p-6 rounded-lg shadow-lg max-w-2xl mx-auto">
        <h1 class="text-3xl font-bold mb-6 text-center">Edit Profile</h1>
        <form action="/contacts/<%= contact.id %>/edit" method="POST" enctype="multipart/form-data">
          <% if (errors) { %>
          <ul class="mb-4">
            <% errors.forEach(error => { %>
            <li class="text-red-500"><%= error.msg %></li>
            <% }) %>
          </ul>
          <% } %>
          <div class="flex flex-col items-center mb-6">
            <img src="" alt="Profile Photo" class="w-32 h-32 rounded-full object-cover mb-4">
            <label for="profile_photo" class="bg-gray-200 px-4 py-2 rounded-lg cursor-pointer hover:bg-gray-300">
              <input type="file" name="profile_photo" id="profile_photo" class="hidden">
              Change Photo
            </label>
          </div>
          <div class="mb-6">
            <label for="nama_lengkap" class="block text-gray-700 font-semibold">Nama Lengkap</label>
            <input type="text" name="nama_lengkap" id="nama_lengkap" class="form-input mt-2 block w-full border-gray-300 rounded-lg" value="<%= contact.nama_lengkap %>" />
          </div>
          <div class="mb-6">
            <label for="nomor_telepon" class="block text-gray-700 font-semibold">Nomor Telepon</label>
            <input type="text" name="nomor_telepon" id="nomor_telepon" class="form-input mt-2 block w-full border-gray-300 rounded-lg" value="<%= contact.nomor_telepon %>" />
          </div>
          <div class="mb-6">
            <label for="email
